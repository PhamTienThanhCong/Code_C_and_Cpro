#ifndef NOTEBOOKWRITING_H
#define NOTEBOOKWRITING_H

#include <string>

class NotebookWriting
{
    public:
        NotebookWriting();
        virtual ~NotebookWriting();
        void writeToNotebook(std::string text);

    protected:

    private:
};

#endif // NOTEBOOKWRITING_H
